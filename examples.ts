export function showUnionExample() {
    // Union type is where the value can either one or more types, in this specific case example can either be a string or boolean
    let example = Math.random() > 0.5 ? "Simon" : false;
    return example;
}

